# AutoClaw — Pickup / What Remains (2026-06-22)

State at writing: **v3.6.4 LIVE on both registries** (manual publish). `master` ==
`dev-beta` (content-identical, `7328e82` / `f9efeb0`), no open PRs, tree clean
(only untracked `research/` + `semantic-review/` scratch). The intelligence
context-pack delivery initiative is fully shipped (PRs #33–#39).

Legend: **[verified 06-22]** = confirmed against the code this session ·
**[from memory — verify]** = recall that may be stale; re-check at session start.

---

## 0. Start-of-session ritual
- `git fetch && git checkout dev-beta && git pull` (dev-beta is the integration trunk).
- `npm run compile` should be clean with existing `node_modules` (no install needed).
- Releases are **manual** (`npm run package` → verify `.vsix` size/contents →
  `node scripts/publish-vsce.js` + `publish-ovsx.js`). PATCH bumps only.
- **Always verify the `.vsix` before publishing** — see §2.3 (the 680 MB scare).

---

## 1. Maintainer-input items (blocked on a human, not code)

### 1.1 Monetization go-live — **[verified 06-22]**
`src/support/supportConfig.ts` still ships placeholders:
`donationUrl` / `customAmountUrl` / `proUrl` = `https://square.link/u/${PLACEHOLDER}`,
`cryptoWallets` = placeholders. `koFiUrl` (`ko-fi.com/gozippy`) is the only real one.
- **Action:** paste real Square links + crypto wallet addresses (in the file
  defaults or via `autoclaw.support.*` settings), and decide which hosted feature
  `requireHosted()` gates first.

### 1.2 Unified license server — **[from memory — verify]**
Decision pending: tag AutoClaw onto the existing DRK license server
(license.gozippy.com, Proxmox CT-94002) for one Zippy license server. ~90%
reusable; needs go/no-go + key strategy (recommend per-product RS256 keypair).
License private key is at `C:\Users\gotad\.autoclaw\secrets\…pem` (env-pointed).

---

## 2. Release / CI / repo hygiene (small, high-value)

### 2.1 `publish.yml` tag-CI red on manual releases — **DONE (dev-beta `5c7e0cc`)**
The `v*` tag re-ran `vsce publish` on an already-(manually-)published version →
failed every release (v3.6.2/3/4 red). Fixed: publish + GitHub-release steps are
now gated behind the repo Actions var `CI_PUBLISH` (default off), so a tag builds
+ tests + packages + uploads the `.vsix` artifact GREEN with publishing staying
manual; package step uses `--no-dependencies`. Takes effect at the next release
(when the fix reaches a tagged master commit). To let CI publish a future tag:
set repo var `CI_PUBLISH=true` and don't pre-publish that version.

### 2.2 Branch + worktree pruning (deferred by policy) — **[verified 06-22]**
Policy (user, 06-22): **keep** merged work-branches per shipped build for
ticket→branch→fix tracing; prune only **after the next version ships**.
- v3.6.4 branches to prune next: `feat/intel-context-packs`,
  `feat/intel-context-autorefresh`, `feat/intel-refresh-service`,
  `docs/intelligence-readme`, `feat/autobuild-step-conditions`,
  `chore/release-3.6.4`; local `wip/autobuild-step-conditions` (`4d4f80b`,
  superseded by #37) is safe to delete.
- Stale worktrees: `k:/tmp/autoclaw-rel` (`fix/packaged-runtime`, merged via #29
  — removable) and `.kilo/worktrees/radical-bakery` (`d607626` — assess/merge or
  remove). `git worktree remove --force <path>`.

### 2.3 Guard against `.vsix` bloat recurrence — **[verified 06-22]**
Root `research/` (680 MB untracked scratch) packaged into the artifact until
`.vscodeignore` was fixed (vsce honors `.vscodeignore`, not `.gitignore`).
- **Action:** add a packaging smoke that fails if the `.vsix` exceeds a sane cap
  (e.g. > 10 MB) so contamination can never ship silently. (Fix already in
  master+dev-beta; this is prevention.)

---

## 3. Premium / public-private split

### 3.1 3rd premium engine — advanced orchestration — **[verified 06-22]**
`src/premium/premiumApi.ts` currently declares ONE engine
(`generatePrEvidenceReport`). Engines so far: PR-evidence (private repo) + Agent
Scorecards (gated command). The next premium engine is **advanced orchestration**.
- **Action:** build it in the **private** `@autoclaw/premium` repo behind the
  existing seam — add the method to the `PremiumApi` interface, a free fallback
  in `src/premium/unavailablePremium.ts`, and the public stub/command. Keep the
  paid implementation OUT of the public repo (the `check-no-secrets.js` guard
  enforces this; verify the `.vsix` ships only the stub).

### 3.2 Public/private discipline (ongoing) — **[user directive]**
Every new paid/proprietary feature: private repo + public stub linked via the
`src/premium/index.ts` `require('@autoclaw/premium')` seam (absent → free
fallback). Users see only what we expose.

---

## 4. Intelligence layer follow-ups

### 4.1 Always-on "watch service" — **[roadmap]**
Today: `/learn` + `/index-code` (manual) + per-host refresh service (tick-based,
`src/intelligence/refreshService.ts`). Missing: a file watcher that re-learns /
re-indexes incrementally **as you work**. Natural home: KDream daemon loop (also
not yet a continuous service) or a debounced `vscode.FileSystemWatcher`.

### 4.2 Knowledge graph population — **PARTLY DONE (dev-beta, PR #40 `db85de8`)**
Was: context packs returned 0 KG facts (nothing wrote to `kg.db` in normal work).
Now: `/learn` records multi-agent **consensus outcomes** as durable `decision`
thoughts (`src/intelligence/kgRecord.ts` `recordCoordinationToKg`, deduped,
degrade-safe). So packs/`kg.search` surface real team decisions once `/learn` runs
on a repo with a comms tree.
- **Still open:** record on **dispatch** + **review** in real time (not just at
  `/learn`), and consider recording decisions/findings beyond consensus
  verdicts. Bi-temporal `valid_to` on superseded verdicts is not set yet.

### 4.3 Intelligence management UI + tiered storage — **[from memory — verify]**
Design exists (`docs/ideas/INTELLIGENCE-MANAGEMENT-AND-TIERED-STORAGE.md`):
visibility/management panel, user-controlled storage location (never force C:),
two-tier project + system intelligence. Build the UI.

---

## 5. Orchestration / fleet / MCP open items

### 5.1 Board auto-transition (#11, deeper) — **[from memory — verify]**
Auto-move board tasks across lifecycle states on coordination signals
(task_complete → review → merged). The other session flagged this as a next pick.

### 5.2 MCP stubs still `not_implemented` — **[verified 06-22]**
`src/mcp/tools.ts` `todo.list` returns `not_implemented` "until the spider
lands" (TODO/AI-item discovery spider). Several BP2/BP3 write-tool stubs +
`src/mcp/install-extended.ts` carry TODOs. Decide which to finish.

### 5.3 Orchestrate gaps + Phase 4 — **[from memory — verify; some likely done]**
`project_orchestrate_gaps` lists P1-10 + P2 items; `project_phase2_part_b`
mentions a `capability_query` handler (refs now exist in comms/extension/
orchestrate — likely partial), cross-machine routing, and Phase 4. Re-audit
against current code before acting — FF-1 (`presence.beacon`) and the embeddings
install gap are already DONE despite older memory.

---

## 6. Separate initiative (not AutoClaw)

`project_gozippy_initiative`: rebuild GoZippy.com (anti-infinite-scroll SPA) +
embeddable feedback/bug SDK + support/ticketing + AI-agent-ops. Workspace
`S:\Projects\GoZippy`; site scaffolded (Astro), themed to brand. Tracked
separately from AutoClaw.

---

## Progress this session (2026-06-22, after v3.6.4)
- ✅ §2.1 publish.yml hygiene — dev-beta `5c7e0cc`.
- ✅ §4.2 KG population (consensus outcomes via /learn) — PR #40, dev-beta `db85de8`.
- dev-beta is now ahead of master by these + the pickup doc; rolls to master at the next release (v3.6.5).

## Suggested first moves for the next session
1. **3rd premium engine** (§3.1) — the headline forward feature, on the private `@autoclaw/premium` repo.
2. **KG population, real-time** (§4.2 "still open") — record on dispatch + review, not just at /learn.
3. If maintainer is available: **monetization links** (§1.1) — unblocks revenue.
4. **Intelligence watch service** (§4.1) or **board auto-transition** (§5.1).

When the next version is cut, run the branch/worktree prune (§2.2).
