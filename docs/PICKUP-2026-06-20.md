# AutoClaw — Pick-up state (2026-06-20)

Reconstructed from a sweep of all 41 Claude Code chat transcripts for this
project after a VS Code restart dropped most open chats. Findings were verified
against live git + the two extension registries, then acted on. This file is the
durable record so the work survives the next lost-chat event.

## TL;DR — what changed today

- **v3.6.1 is now PUBLISHED to both registries.** It had been merged + tagged on
  `origin/master` (`2d3abfd`) since 6/19 but never published — the registries were
  stuck at v3.5.0. Published the clean, pre-built, tree-identical artifact from the
  `k:\tmp\autoclaw-rel` worktree (`vsce` + `ovsx` both returned success).
- **License signing private key secured.** Moved out of `k:\tmp` (world-readable
  scratch) to `C:\Users\gotad\.autoclaw\secrets\autoclaw-license-private-key.pem`
  with the ACL locked to the user account. Point `AUTOCLAW_LICENSE_PRIVATE_KEY` at
  that path when running `scripts/sign-license.js`. Also deleted the leftover
  publish-token `.env` from the scratch worktree.

## Tier 2 progress (2026-06-20, same session)

Shipped on `feat/multi-project-orchestration` (1868 unit tests passing; the lone
red is a known Windows PeerServer socket flake, unrelated):

- **#4 Automatic token writer — DONE** (`a1c2c60`). `recordDispatchCost()` turns a
  completed `DispatchResult` into a per-agent cost-ledger entry (skips failed /
  token-less results; never throws).
- **#5 Runner dispatch contract — DONE** (`a1c2c60`). New `dispatchViaRegistry()`
  makes `getPreferred()`/`dispatch()` reachable + tested; wired into the
  `spawn_runner` hook behind `AUTOCLAW_RUNNER_DIRECT_DISPATCH=true` (off by
  default — work-queue path stays default), auto-feeding the cost ledger.
- **#6 Consensus revise/converge loop — DONE** (`d9135cb`). On a dissent verdict
  with rounds left, the author gets a `revision_request` and the panel re-votes;
  bounded by `reviseMaxRounds` (loop opts in at 2). Back-compatible (default 1).
- **#8 Intelligence panel backend auto-detect — DONE** (`d147285`). Panel detects
  the vector backend on refresh and shows a green "● Online" pill / hides the
  Deploy CTA accordingly.
- **#7 Monetization — BLOCKED on you.** The Ko-fi rail is live; the rest needs
  your Square account links + crypto wallet addresses (paste into
  `src/support/supportConfig.ts` defaults or the `autoclaw.support.*` settings)
  and a decision on which hosted feature `requireHosted()` should gate first.

## Verified ground truth (as of 2026-06-20)

| Thing | State |
|---|---|
| VS Code Marketplace | v3.6.1 published (was 3.5.0; propagation may lag a few min) |
| Open VSX | v3.6.1 published (was 3.5.0) |
| `origin/master` | v3.6.1 (`2d3abfd`) |
| `origin/dev-beta` | 8 commits behind master (master is the current trunk) |
| working branch `feat/multi-project-orchestration` | pkg 3.5.5; was 27 behind / 5 ahead of master before today's forward-merge |

The 5 branch-only commits are the Manager Surface / session-tracking / token-context
work: `baa5761 ca5bef7 1dc4d50 5e12e2d b34dfbe`.

## Already shipped — do NOT redo

Everything that the 6/13–6/16 chats described as "held release / pending merge"
landed on master and is in v3.6.1: v3.4.0/v3.5.0 releases, wave-b push, KG
in-process rebuild (PR #24), embeddings guided-install (PR #22/#26), Ko-fi (PR #23),
the universal embeddings ladder, fleet federation, and the packaged-runtime fix
(vendored `ws`/`chokidar` + section-search assets, PR #29). Most May items are
superseded by shipped versions.

## Open work — what's left

### Tier 1 (highest value)
1. ~~Publish v3.6.1 to both registries~~ — **DONE 2026-06-20.**
2. ~~Move license private key out of scratch~~ — **DONE 2026-06-20.**
3. **Forward-merge `origin/master` into `feat/multi-project-orchestration` + push.**
   In progress today — brings the branch current with v3.6.1 and pushes the 5
   panel/session-tracking commits so they stop living only on this machine.

### Tier 2 (substantial features)
4. **Automatic token writer.** The per-agent cost ledger renders and the append seam
   is tested, but there is no in-process `DispatchResult` consumer that actually
   fills it. Needs heartbeat token deltas or an LLM-ledger→agent join.
   (src/fleet/* cost rollup; session `eccaaeae`.)
5. **Wire the runner/bridge contract.** `createDefaultRunnerRegistry()` is built but
   `.dispatch()` / `getPreferred()` have zero runtime callers; the `spawn_runner`
   hook action is unplumbed ("no spawnRunner dep wired"); the Kilo §6 bridge
   companion (`adapters/kilocode/bridge-companion/`) is README-only. (session `794de57f`.)
6. **Auto-consensus revise/converge loop.** Four protocol gaps, none built: no second
   "respond-to-dissent" round before tally; reviews not grounded by file:line; no
   one-shot ad-hoc dispatcher (must author a full `/orchestrate` manifest); read-only
   agendaboard. This is the "stop being the human message-broker" feature.
   (session `d5f83846`; `docs/AGENT_SESSION_PROTOCOL.md`.)
7. **Finish monetization.** Code is in; needs maintainer inputs: real **Square links**
   (the gating input), crypto wallet addresses, `REPLACE_ME` placeholders in
   SUPPORT.md / PRICING.md / FUNDING.yml, and one reference `requireHosted()`
   integration. Ko-fi already shipped. (sessions `7919784b`, `385f28af`.)
8. **Intelligence panel UI overhaul.** Status/backend section is too tall; needs
   workspace-vs-global backend auto-detection + conditional Deploy-backend button +
   compact green "online" indicator. Was gated on autobuild workflow `w54de3mvz`
   (verify whether it ever completed). (session `4e807e38`;
   `feat/intelligence-panel-buttons`.)

### Tier 3 (verify-then-act)
9. **"Awaiting You" human-task panel.** Clicking a human-awaiting task shows a dead
   reply box (no history, reply does nothing). A 5-file fix was wiped by a
   `radical-bakery` merge and never re-applied — verify against current panel code.
   (session `6d6f0c1c`.)
10. **Bridge port collision** (`EADDRINUSE 127.0.0.1:19880`) when the same project is
    open in both VS Code and Kiro. kg-daemon got a port fallback; the bridge may not.
    Verify whether `src/bridge.ts` auto-increments past a busy port. (session `d0760af7`.)
11. **Connect Hermes / OpenClaw agents to boards** — the originating question in
    `eccaaeae` was never answered.
12. **Aikido SAST disposition** — confirmed false-positive path-traversal at
    `systemStore.ts:56/225` + `ide-ports.ts:53`; choose suppress-in-UI vs inline-ignore
    vs basename-assert. (session `9a3174bf`.)
13. **Worktree cleanup** — `k:\tmp\autoclaw-{devbeta,rel,kofi,test}` are stale.

## Decisions owed by the maintainer
- Square payment links (gates the whole monetization rail).
- Ship a minimum-viable Kilo/Kiro bridge extension by default? (session `bdd00453`.)
- Green-light REP-2 (fold `reputationFactor` into `scoreAgent` in `src/orchestrate.ts`)?
  (session `ad00e487`.)

## How this was reconstructed
Transcript dir: `C:\Users\gotad\.claude\projects\k--Projects-autoclaw\` (41 `.jsonl`).
Ranked by last *genuine* message timestamp (file mtimes were unreliable — a VS Code
restart touched ~25 files at once writing `ai-title`/`mode`/`last-prompt` metadata).
Distilled each into a digest (user prompts + final assistant summary + final todo
state), mined them with parallel agents, then verified findings against git + the
live registries.
