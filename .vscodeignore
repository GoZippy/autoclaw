# Secrets - never ship credentials
.env
.env.local
.env.*.local
.mcp.json

# Dev source, tests, docs, scripts - ship compiled out/ only
src/**
test/
tests/
scripts/**
packages/**
*.vsix
tsconfig.tsbuildinfo

# Compiled test and script artifacts
out/test/**
out/scripts/**

# Source maps and TS artifacts in out/
out/**/*.map
out/**/*.ts

# Dev tooling
.vscode/
.vscode-test/
.yarn/

# Runtime workspace data - not for distribution
.autoclaw/
.claude/**
.clinerules/**
.kilo/**
.kilocodemodes
.voidspec/**
.cursor/**
.cursorignore

# CI/CD internals
.github/**
node_modules/.package-lock.json

# Intelligence Layer native peers — declared as optionalDependencies but NOT
# bundled. They follow the packages/kg-daemon native-peer model: maintainer-/
# user-installed locally, lazy-required only when a vector backend is used. The
# always-available `none` embedding provider needs none of them, so excluding
# them keeps the .vsix lean and packaging reliable.
node_modules/better-sqlite3/**
node_modules/sqlite-vec/**
node_modules/@xenova/**
node_modules/pg/**
node_modules/pg-*/**
node_modules/onnxruntime-node/**
node_modules/onnxruntime-common/**
node_modules/@huggingface/**

# OS junk
.DS_Store
Thumbs.db

# Root dotfiles not needed by extension consumers
.gitignore
.gitattributes
.npmrc
.husky/
lint-staged*
.prettier*
.eslint*
.editorconfig
.babelrc
.travis.yml
.circleci/
dangerfile*
rollup.config*
webpack.config*
jest.config*
babel.config*
commitlint*

# Local scratch / research
tmp/**

# Internal documentation - keep README, CHANGELOG, LICENSE only
docs/research/**
docs/specs/**
docs/IDEAS_LOG.md
docs/COORDINATION_IMPROVEMENTS.md
docs/DISTRIBUTED_AGENT_FABRIC.md
docs/otherProjects.md
docs/otherProjects-catalog.md
docs/plan.md
docs/CROSS_AGENT_ARCHITECTURE.md
ORCHESTRATION-IMPROVEMENTS.md
docs/V3_PLAN.md
docs/V3_1_ROADMAP.md
docs/AGENT_DAEMON_CRITIQUE.md
docs/rfc/llm-provider-abstraction.md

# Relay server (AF-10) — a separate self-hostable deployable, not extension code
out/relay-server/**
src/relay-server/**
