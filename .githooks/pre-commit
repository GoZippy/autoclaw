#!/bin/sh
# AutoClaw public-repo pre-commit guard. Blocks secrets + private/paid code from
# entering staged commits. Opt in once per clone:
#   git config core.hooksPath .githooks
# (CI enforces the same check regardless, via scripts/check-no-secrets.js.)
node scripts/check-no-secrets.js --staged || exit 1
